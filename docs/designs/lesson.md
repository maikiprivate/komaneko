# 駒塾（Lesson）設計

## 概要

Duolingo Chess を参考にした将棋学習システム。駒の動かし方から定跡・手筋まで幅広く学べる。

## 設計方針

### コンテンツ構造

```
Course（コース）
├── Section（セクション）
│   ├── Lesson（レッスン）
│   │   ├── Problem 1
│   │   ├── Problem 2
│   │   └── ...（5-10問）
│   └── ...
└── ...
```

### 正解判定アプローチ

詰将棋とは異なり、**事前定義の手順と比較**して正解を判断する。

```
1. ユーザーが駒を動かす
2. 事前定義の正解手順と比較
   - 一致 → 次の手へ（または正解）
   - 不一致 → 不正解
3. 必要手数を満たしたら → 正解！
```

複数の正解手順がある場合は、`correctMoves: Move[][]` で複数パターンを定義。

## データ構造

### 型定義

```typescript
/** コース */
interface Course {
  id: string
  title: string           // 「駒の動かし方」
  description: string
  sections: Section[]
  status: 'locked' | 'available' | 'in_progress' | 'completed'
}

/** セクション */
interface Section {
  id: string
  title: string           // 「歩と金」
  lessons: Lesson[]
  status: 'locked' | 'available' | 'in_progress' | 'completed'
}

/** レッスン */
interface Lesson {
  id: string
  title: string           // 「歩の基本」
  problems: Problem[]
  status: 'locked' | 'available' | 'in_progress' | 'completed'
}

/** 問題（盤面操作型） */
interface BoardProblem {
  id: string
  type: 'board'
  sfen: string              // 初期盤面
  instruction: string       // 「歩を1マス前に動かしてください」
  correctMoves: Move[][]    // 正解手順（複数パターン可）
  movesRequired: number     // 必要手数（1手〜複数手）
}

/** 問題（座学型）- 将来拡張用 */
interface QuizProblem {
  id: string
  type: 'quiz'
  question: string
  options: string[]
  correctIndex: number
}

type Problem = BoardProblem | QuizProblem
```

### 初回コース: 駒の動かし方

```typescript
{
  id: 'piece-movement',
  title: '駒の動かし方',
  sections: [
    {
      id: 'fu-kin',
      title: '歩と金',
      lessons: [
        {
          id: 'fu-basics',
          title: '歩の基本',
          problems: [
            {
              id: 'fu-1',
              type: 'board',
              sfen: '9/9/9/9/4P4/9/9/9/9 b - 1',
              instruction: '歩を1マス前に動かしてください',
              correctMoves: [[{ type: 'move', from: {row:4,col:4}, to: {row:3,col:4}, piece: 'fu', promote: false }]],
              movesRequired: 1,
            },
            // ... 5-10問
          ],
        },
        // ... 金の基本
      ],
    },
    // ... 他のセクション
  ],
}
```

### 駒の学習順序

```
歩 → 金 → 銀 → 香 → 飛 → 角 → 桂 → 王
```

シンプルな動き → 複雑な動きの順。

## 画面構成

### 1. コース一覧画面（`/lesson` タブ）

```
+----------------------------------+
| [LogoHeader]                     |
+----------------------------------+
|                                  |
|  ┌────────────────────────────┐  |
|  │ 駒の動かし方               │  |
|  │ 基本の駒の動きを学ぼう     │  |
|  │ ████████░░░░░░░░  50%      │  |
|  └────────────────────────────┘  |
|                                  |
|  ┌────────────────────────────┐  |
|  │ 🔒 基本手筋                │  |
|  │ （準備中）                 │  |
|  └────────────────────────────┘  |
|                                  |
+----------------------------------+
```

### 2. セクション一覧画面（`/lesson/[courseId]`）

Duolingo風のツリー表示。

```
+----------------------------------+
| ← 駒の動かし方                   |
+----------------------------------+
|                                  |
|        ○ 歩と金 ✓                |
|        │                        |
|        ○ 銀と桂 🔓               |
|        │                        |
|        ○ 香と飛 🔒               |
|        │                        |
|        ○ 角と王 🔒               |
|                                  |
+----------------------------------+
```

### 3. レッスン画面（`/lesson/[courseId]/[lessonId]`）

詰将棋画面と同じフッター（やり直し/ヒント/解答）を使用し、UIを共通化する。

```
+----------------------------------+
| ×                   1/5 ████░░░  |
+----------------------------------+
| 🐱「歩を1マス前に動かしてにゃ！」 |
+----------------------------------+
|       相手駒台                    |
|       +-------------+            |
|       |             |            |
|       |   将棋盤    |            |
|       |             |            |
|       +-------------+            |
|       自分駒台                    |
+----------------------------------+
| [やり直し] | [ヒント] | [解答]   |
+----------------------------------+
```

**詰将棋との共通点:**
- フッター（やり直し/ヒント/解答）
- 正解/不正解フィードバック（○×表示）
- 成り選択ダイアログ
- 駒猫コメント

**詰将棋との違い:**
- ヘッダー: タイトル → ×ボタン + プログレスバー
- 進捗表示: 手数カウント → 問題番号/全問題数

### 4. 結果画面

```
+----------------------------------+
|                                  |
|          レッスン完了！          |
|                                  |
|            ★★★                 |
|                                  |
|         正解: 4/5問              |
|                                  |
|  ┌────────────────────────────┐  |
|  │   間違えた問題を復習する   │  |
|  └────────────────────────────┘  |
|                                  |
|  ┌────────────────────────────┐  |
|  │      次のレッスンへ        │  |
|  └────────────────────────────┘  |
|                                  |
+----------------------------------+
```

## 実装ステップ

**方針**: 画面先行で実装し、データ構造の過不足を判断してから確定する

### Step 1: レッスン画面（UI先行）✅ 完了

**ファイル:** `packages/app/app/lesson/[courseId]/[lessonId].tsx`

- プログレスバー
- 駒猫による問題文表示
- 盤面操作（詰将棋ロジック流用）
- 正解/不正解フィードバック
- ハードコードしたデータで動作確認

### Step 1.5: UI共通化

**目的:** 詰将棋画面とレッスン画面で共通のUIコンポーネントを抽出

**作成ファイル:**
- `components/shogi/GameFooter.tsx` - やり直し/ヒント/解答ボタン
- `components/shogi/FeedbackOverlay.tsx` - ○×フィードバック
- `components/shogi/PromotionDialog.tsx` - 成り選択ダイアログ

**修正ファイル:**
- `app/tsumeshogi/[id].tsx` - 共通コンポーネント使用
- `app/lesson/[courseId]/[lessonId].tsx` - 共通コンポーネント使用

### Step 2: 結果画面

**ファイル:** `packages/app/app/lesson/result.tsx`

- 正答数表示
- 「間違えた問題を復習」ボタン
- 「次のレッスンへ」ボタン

### Step 3: 一覧画面

**ファイル:**
- `packages/app/app/(tabs)/lesson.tsx` - コース一覧
- `packages/app/app/lesson/[courseId]/index.tsx` - セクション一覧

### Step 4: データ構造・モックデータ確定

**ファイル:** `packages/app/mocks/lessonData.ts`

画面実装で判明した要件を元に型定義を確定：
- Course, Section, Lesson, Problem の型定義
- 「駒の動かし方」コースのモックデータ（歩・金のみで最小限）

### Step 5: useLessonGame フック

**ファイル:** `packages/app/hooks/useLessonGame.ts`

```typescript
interface UseLessonGameReturn {
  boardState: BoardState
  currentProblemIndex: number
  totalProblems: number
  gameState: 'playing' | 'correct' | 'incorrect' | 'completed'
  instruction: string

  // セッション結果
  results: { problemId: string; correct: boolean }[]

  handleCellPress: (row: number, col: number) => void
  handleCapturedPress: (pieceType: PieceType) => void
  handlePromotionSelect: (promote: boolean) => void

  nextProblem: () => void
  retry: () => void
  skip: () => void
}
```

### Step 6: モックデータ拡充

- 残り6駒のデータ追加（銀、桂、香、飛、角、王）

## ファイル構成

```
packages/app/
├── app/
│   ├── (tabs)/
│   │   └── lesson.tsx              # コース一覧
│   └── lesson/
│       ├── [courseId]/
│       │   ├── index.tsx           # セクション一覧
│       │   └── [lessonId].tsx      # レッスン画面
│       └── result.tsx              # 結果画面
├── hooks/
│   └── useLessonGame.ts            # レッスンロジック
├── mocks/
│   └── lessonData.ts               # モックデータ
└── components/
    ├── shogi/                      # 詰将棋・駒塾共通
    │   ├── GameFooter.tsx          # やり直し/ヒント/解答ボタン
    │   ├── FeedbackOverlay.tsx     # ○×フィードバック
    │   └── PromotionDialog.tsx     # 成り選択ダイアログ
    └── lesson/
        ├── CourseCard.tsx          # コースカード
        ├── SectionTree.tsx         # セクションツリー
        └── LessonProgress.tsx      # プログレスバー
```

## 共通コンポーネント（詰将棋・駒塾）

詰将棋画面とレッスン画面で共通のUIコンポーネントを抽出。

### GameFooter

```typescript
interface GameFooterProps {
  onReset: () => void
  onHint: () => void
  onSolution: () => void
}
```

### FeedbackOverlay

```typescript
interface FeedbackOverlayProps {
  type: 'correct' | 'incorrect' | 'none'
}
```

### PromotionDialog

```typescript
interface PromotionDialogProps {
  visible: boolean
  onSelect: (promote: boolean) => void
}
```

## 参照ファイル

| ファイル | 用途 |
|----------|------|
| `hooks/useTsumeshogiGame.ts` | フック設計パターン |
| `app/tsumeshogi/[id].tsx` | 盤面操作UI |
| `components/shogi/ShogiBoard.tsx` | 盤面コンポーネント |
| `lib/shogi/moveGenerator.ts` | 移動可能位置の計算 |
